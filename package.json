{
  "name": "dependency-comparator",
  "version": "1.0.0",
  "description": "Compare the dependencies of two package.json in order to generate a spreadsheet with what is common between the parts and also know what is not there. Among what they have in common, it is possible to know which project has the most updated version of the dependency.",
  "main": "index.js",
  "scripts": {
    "start": "npx ts-node ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rafahlobo/dependency-comparator.git"
  },
  "keywords": [
    "dependency",
    "comparator",
    "nodejs",
    "package.json"
  ],
  "author": "Rafael Lobo",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rafahlobo/dependency-comparator/issues"
  },
  "homepage": "https://github.com/rafahlobo/dependency-comparator#readme",
  "dependencies": {
    "commander": "^12.0.0",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  }
}
